/*let student = {
    name: 'Lucas',
    favouritefood: 'pâtes',
    city: 'Verneuil', 
}
 console.log(Object.values(student.name).length)
 console.log(Object.values(student.favouritefood).length)
 console.log(Object.values(student.city).length)
 
 let a= Object.values(student.name).length
 let b= Object.values(student.favouritefood).length
 let c= Object.values(student.city).length
 let d= a+b+c
 console.log(d)
 if(d%2 == 0) {
    console.log('pair')
 } else {
    console.log('impair')
} 


let student = {
name: 'Lucas',
favouritefood: 'pâtes',
city: 'Verneuil', 
}

let values = Object.values(student)
let nombreDElettres = 0

values.forEach((value) => {
    nombreDElettres += value.length
})
if (nombreDElettres % 2 === 1) {
    console.log('pair')
} else {
console.log ("impair")
}
*/